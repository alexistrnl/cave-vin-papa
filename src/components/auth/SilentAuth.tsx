"use client";

// Ce composant est maintenant remplacé par AuthProvider dans le layout
// Conservé pour compatibilité mais ne fait plus rien (la logique est dans AuthContext)
export default function SilentAuth() {
  return null;
}
